---
phase: 03-capture
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/capture/ReflectionEditor.tsx
  - src/lib/capture-utils.ts
  - src/components/card/CardBack.tsx
  - src/components/grid/WeekGrid.tsx
autonomous: false
requirements: [CAPT-03, CAPT-05]

must_haves:
  truths:
    - "User can write a longer reflection in a dedicated space visually distinct from quick capture"
    - "Grid cells update color from empty to has-captures in real-time when a new entry is added"
    - "Both quick captures and reflections appear in the card back entry list with distinct icons"
  artifacts:
    - path: "src/components/capture/ReflectionEditor.tsx"
      provides: "Multi-line textarea for longer reflections with autosave"
      min_lines: 40
    - path: "src/lib/capture-utils.ts"
      provides: "Extended with createReflection function"
      exports: ["createQuickCapture", "createReflection"]
    - path: "src/components/grid/WeekGrid.tsx"
      provides: "Grid cells reactively update when entries are added (already uses useLiveQuery)"
  key_links:
    - from: "src/components/capture/ReflectionEditor.tsx"
      to: "src/lib/capture-utils.ts"
      via: "calls createReflection on save"
      pattern: "createReflection"
    - from: "src/lib/capture-utils.ts"
      to: "db.entries"
      via: "Dexie add() for reflection entries with type='reflection'"
      pattern: "db\\.entries\\.add"
    - from: "src/components/grid/WeekGrid.tsx"
      to: "db.entries"
      via: "useLiveQuery Set of weekIds updates when new entries are added"
      pattern: "useLiveQuery"
---

<objective>
Add dedicated reflection editor for longer-form writing and verify grid cells update in real-time when captures are added.

Purpose: Quick capture handles fleeting thoughts, but users also need a space for deeper weekly reflections. The grid must reflect new data immediately so the user sees their life filling in as they write.

Output: ReflectionEditor component on card back (toggled via tab), grid cells changing from empty to has-captures state reactively.
</objective>

<execution_context>
@/Users/fangbotao/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fangbotao/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-capture/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan depends on -->

From src/lib/capture-utils.ts (created in Plan 01):
```typescript
export function createQuickCapture(params: { content: string; weekNumber: number }): Promise<Entry>;
```

From src/lib/types.ts:
```typescript
export interface Entry {
  id: string;
  weekId: string;       // "week-{weekNumber}"
  content: string;
  type: "quick" | "reflection";
  createdAt: string;
  updatedAt: string;
  deviceId: string;
}
```

From src/components/grid/WeekGrid.tsx (from 02-01-SUMMARY):
- Uses `useLiveQuery` to build a Set of weekIds that have entries
- Passes week state to WeekCell which colors based on: empty, has-captures, synthesized, current, future
- The Set is built from `db.entries.orderBy('weekId').uniqueKeys()` or similar
- When a new entry is added to Dexie, useLiveQuery automatically re-fires and the Set updates

From src/components/card/CardBack.tsx:
- Already displays entries sorted by createdAt with type icons (MessageCircle for quick, PenLine for reflection)
- Plan 01 adds QuickCapture at bottom for current week
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReflectionEditor and extend capture-utils</name>
  <files>src/components/capture/ReflectionEditor.tsx, src/lib/capture-utils.ts</files>
  <action>
Extend `src/lib/capture-utils.ts`:
- Add `createReflection` function accepting `{ content: string, weekNumber: number }`
- Same logic as createQuickCapture but sets `type: "reflection"`
- Refactor: extract a shared `createEntry` internal function that both createQuickCapture and createReflection call, differing only in `type`

Create `src/components/capture/ReflectionEditor.tsx`:
- Props: `{ weekNumber: number }`
- Multi-line textarea (shadcn-style, stone palette) with minimum 4 rows, resizable vertically
- Placeholder: "Reflect on your week..."
- Save button below the textarea (not Enter — Enter creates newlines in multi-line text)
- Autosave with debounce: save draft to a Zustand store or local ref so content is not lost if user accidentally closes the card. On component mount, restore any in-progress draft for this weekNumber.
  - Simple approach: use a `useRef` to track content, and save to localStorage key `weeky-reflection-draft-${weekNumber}` on every change (debounced 500ms). On mount, load from localStorage. Clear localStorage key after successful save to Dexie.
- Save button calls `createReflection({ content, weekNumber })` then clears the textarea and draft
- Save button disabled when textarea is empty or whitespace-only
- Visual distinction from QuickCapture: different background tint, PenLine icon in header, label "Weekly Reflection"
  </action>
  <verify>
    <automated>cd /Users/fangbotao/Claude_Code/weeky && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>ReflectionEditor renders a multi-line textarea with autosave draft protection. createReflection persists entries with type="reflection" to IndexedDB.</done>
</task>

<task type="auto">
  <name>Task 2: Add capture mode toggle to CardBack and verify grid reactivity</name>
  <files>src/components/card/CardBack.tsx, src/components/grid/WeekGrid.tsx</files>
  <action>
Modify `src/components/card/CardBack.tsx`:
- Add a simple toggle between "Quick" and "Reflect" modes at the bottom of the card (only for current week)
- Use local state: `captureMode: 'quick' | 'reflect'`
- When mode is 'quick', show QuickCapture (from Plan 01)
- When mode is 'reflect', show ReflectionEditor
- Toggle UI: two small tab-style buttons ("Quick" with MessageCircle icon, "Reflect" with PenLine icon) above the capture area
- Default to 'quick' mode
- Layout: entries list (scrollable) on top, then mode toggle tabs, then the active capture component, then flip hint at very bottom

Verify grid reactivity in `src/components/grid/WeekGrid.tsx`:
- Read the existing useLiveQuery that builds the Set of weekIds with entries
- Confirm it uses a pattern like `db.entries.orderBy('weekId').uniqueKeys()` or equivalent that Dexie will re-fire when entries are added
- If the query correctly builds a reactive Set, no changes needed — adding an entry via QuickCapture or ReflectionEditor will automatically update the grid cell from "empty" to "has-captures" color
- If the query is NOT reactive to new weekIds (e.g., it caches), fix it to ensure reactivity
- Test: after build, document that adding a capture for the current week should change the grid cell color from stone (empty) to amber (has-captures)

Run `npm run build` to verify everything compiles.
  </action>
  <verify>
    <automated>cd /Users/fangbotao/Claude_Code/weeky && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>CardBack shows Quick/Reflect toggle tabs for current week. Both capture types create entries visible in the list. Grid cells reactively update color when entries are added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete capture flow</name>
  <files>none (verification only)</files>
  <action>
Human verifies the full capture system end-to-end:
1. Open http://localhost:3000 in the browser
2. Click the current week cell in the grid — card overlay should open
3. Flip to the card back
4. Verify "Quick" mode is active with a chat-style input at the bottom
5. Type "Testing quick capture" and press Enter or click Send
6. Verify the entry appears immediately in the list above with a timestamp and chat bubble icon
7. Switch to "Reflect" tab
8. Type a longer reflection (multiple lines) in the textarea
9. Click Save — verify it appears in the list with a pencil icon
10. Close the card and verify the grid cell for the current week changed from stone/empty to amber/has-captures color
11. Refresh the page — reopen the card — verify all entries are still there (persistence)
12. Click a past week cell — verify no capture input appears (read-only)
  </action>
  <verify>
    <automated>cd /Users/fangbotao/Claude_Code/weeky && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms: quick capture works, reflection works, grid updates live, data persists, past weeks are read-only</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no type errors
2. `npm run build` completes successfully
3. ReflectionEditor component exists and uses createReflection
4. CardBack has Quick/Reflect mode toggle for current week only
5. Grid cells reactively change from empty to has-captures when entries are added
6. Human verification of full capture flow passes
</verification>

<success_criteria>
- Quick capture and reflection are visually distinct modes on the card back
- Both create properly typed entries (quick vs reflection) in IndexedDB
- Reflection has autosave draft protection (localStorage debounce)
- Grid cells update from empty (stone) to has-captures (amber) in real-time when any entry is added
- All entries persist across page refreshes
- Past weeks are read-only (no capture input)
- Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-capture/03-02-SUMMARY.md`
</output>
